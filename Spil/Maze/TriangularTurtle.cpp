#include "StdAfx.h"
#include "TriangularTurtle.h"

void TriangularTurtle::firstDirection() {
/*
  const int dir = getCurrentDirection();
  if(dir < 3) {
    turnTo(dir+3);
  } else {
    turnTo(dir-3);
  }
*/
}

void TriangularTurtle::lastDirection() {
/*
  const int dir = getCurrentDirection();
  if(dir >= 3) {
    turnTo(dir-3);
  } else {
    turnTo(dir+3);
  }
*/
}
